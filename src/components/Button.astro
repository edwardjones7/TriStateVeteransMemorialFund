---
/**
 * Button — rendered as an anchor. One primary CTA per screen (red).
 * Meets the 44px minimum touch target.
 */
interface Props {
  href: string;
  variant?: 'primary' | 'secondary' | 'ghost' | 'on-dark' | 'light';
  size?: 'md' | 'lg';
  class?: string;
  external?: boolean;
  'aria-label'?: string;
}
const {
  href,
  variant = 'primary',
  size = 'md',
  class: cls = '',
  external = false,
  'aria-label': ariaLabel,
} = Astro.props;

const base =
  'group inline-flex items-center justify-center gap-2.5 rounded-[3px] font-body font-semibold tracking-tight transition-[background-color,color,transform,box-shadow] duration-150 ease-out-soft active:scale-[0.98]';

const sizes = {
  md: 'px-5 py-2.5 text-base min-h-[44px]',
  lg: 'px-7 py-3.5 text-lg min-h-[52px]',
};

const variants = {
  primary: 'bg-red text-bone hover:bg-red-deep shadow-sm',
  light: 'bg-bone text-navy hover:bg-white shadow-sm',
  secondary:
    'border border-ink/35 text-ink hover:border-ink hover:bg-ink hover:text-bone',
  ghost: 'text-ink hover:text-red px-0',
  'on-dark':
    'border border-bone/40 text-bone hover:bg-bone hover:text-ink',
};

const ext = external
  ? { target: '_blank', rel: 'noopener noreferrer' }
  : {};
---

<a
  href={href}
  class:list={[base, sizes[size], variants[variant], cls]}
  aria-label={ariaLabel}
  {...ext}
>
  <slot />
</a>
